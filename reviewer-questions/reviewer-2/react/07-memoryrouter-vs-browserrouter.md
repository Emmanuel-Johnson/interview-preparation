BrowserRouter is a React Router component used for normal web routing.
It uses the browser’s history API to change URLs without reloading the page.
It is mainly used in web applications with clean URLs.

MemoryRouter is a React Router component that stores routes in memory instead of the browser URL.
It is mainly used for testing or non-browser environments like React Native.
The URL in the address bar does not change when navigating.