Rules of Hooks say hooks should only be called at the top level of a React component or custom hook.
Do not call hooks inside loops, conditions, or nested functions.
This keeps hook execution order consistent in every render.