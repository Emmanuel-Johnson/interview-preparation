Viewport is the visible area of a webpage inside the browser or device screen.
Resolution is the total number of pixels on a screen, like 1920×1080.
Viewport changes with browser size, but resolution is the device’s actual screen size.