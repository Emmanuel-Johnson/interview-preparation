A React element is a simple object that describes what should appear on the UI.
A React component is a function or class that returns React elements.
Elements are the output, components are the creators.