A metaclass in Python is a class that creates and controls other classes.
Just like objects are created from classes, classes themselves are created from metaclasses.
It is mainly used to customize class behavior while the class is being created.