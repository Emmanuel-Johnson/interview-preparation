A context manager in Python is used to manage resources like files or database connections safely.
It works with the with statement and uses __enter__() for setup and __exit__() for cleanup automatically.
This helps prevent resource leaks and makes the code cleaner and safer.